import React, { useState } from "react";
import { Button, Text } from "react-native";
import styled from "styled-components/native";

const App = () => {
  return <Text>Inital commit</Text>;
};

export default App;
